# 이 파일을 .env 로 복사해 값을 채운다. .env 는 .gitignore 에 있어 저장소에 올라가지 않는다.
#
#   cp .env.example .env
#   그리고 아래 값을 채운다
#
# 키 발급 — https://opendart.fss.or.kr  (무료, 하루 20,000회)
DART_API_KEY=

# 참고: 이 파일이 없어도 ../stock_analyzer/.env 의 DART_API_KEY 를 자동으로 쓴다.
#       환경변수 DART_API_KEY 가 있으면 그쪽이 가장 먼저다.
