# 컨텍스트 노트

작업 중 내린 결정과 그 이유를 계속 덧붙인다.

## 2026-08-27 — 착수 시점 상태 파악

프로토타입은 `index.html` 752줄 단일 파일 하나뿐이었다.
CLAUDE.md 가 전제하는 `collect_dart.py`, `events.json`, `.github/workflows/`,
`bio-ir-timeline` 스킬은 전부 존재하지 않았다. 데이터는 HTML 안에 하드코딩된
`EVENTS` 30건(확정 22 / 샘플 8)뿐.

CLAUDE.md 가 `bio-ir-timeline.html` 이라 적어둔 파일의 실제 이름은 `index.html` 이다.
GitHub Pages 배포를 전제하므로 `index.html` 쪽을 정본으로 삼고 문서를 고친다.

## 결정 1 — 연쇄 일정을 중첩 구조로 만들지 않는다

학회는 초록공개→개막, 유상증자는 청약→납입→신주상장처럼 한 사건이 여러 날짜를
갖는다. `phases: [...]` 같은 중첩 필드가 자연스러워 보이지만 CLAUDE.md 의
「데이터는 EVENTS 배열 하나로 흐른다」를 깬다.

각 단계를 **독립 이벤트**로 두고 `chain`(같은 사건 묶음 id) + `chainStep`(단계 이름)
으로 서로를 가리키게 했다. 배열은 평평한 채로 남고, 화면은 같은 `chain` 값을 가진
막대들을 연결선으로 잇는다. 필터·정렬·D-day 계산 로직을 하나도 안 고쳐도 된다.

## 결정 2 — 워치리스트는 수집기가 읽고, 화면은 플래그만 본다

종목 범위를 「워치리스트 + 공통 일정」으로 잡았다.
수집기가 워치리스트를 알고 있으므로 각 이벤트에 `watch:true` 를 찍어 내보낸다.
화면은 종목 목록을 알 필요 없이 이 플래그만 보고 필터한다.
워치리스트가 바뀌면 재수집하면 되고, 화면 코드는 그대로다.

워치리스트 소스 우선순위.
1. `watchlist.json` (이 프로젝트 로컬, 사용자가 직접 관리)
2. 없으면 `stock_analyzer/data/active_universe.json` + `user_picks.json` 병합

## 결정 3 — corp_code 는 로컬 XML 을 먼저 쓴다

DART 종목 조회에는 6자리 종목코드가 아니라 8자리 `corp_code` 가 필요하다.
`corpCode.xml` 은 30MB zip 다운로드다. `stock_analyzer/data/corp_codes.xml` 에
이미 압축 해제된 사본이 있어 그걸 먼저 찾고, 없을 때만 DART 에서 받는다.
파싱 결과는 `data/corp_map.json` 으로 캐시해 매번 30MB 를 다시 읽지 않는다.

## 결정 4 — 법정기한 계산은 키 없이 동작해야 한다

분기·반기 45일, 사업보고서 90일 규칙은 API 호출이 필요 없다.
이 부분만 떼어 `--selftest` 로 검증 가능하게 두면 키 없는 환경(CI 초기 설정,
다른 사람의 클론)에서도 최소한 화면이 비지 않는다.
공휴일 테이블은 코드에 박되, 출처를 주석에 남기고 매년 갱신 대상으로 표시한다.

## 결정 5 — 접수일만으로는 화면이 성립하지 않는다 (수집 결과로 확인)

`--no-doc`로 워치리스트 26종목 × 180일을 훑으니 공시 2,400건 중 627건이
일정 후보로 걸렸는데, 그중 **미래 일정은 14건**뿐이었고 그마저 전부
법정기한과 학회 일정이었다. DART 공시는 이미 벌어진 일의 접수 기록이라
접수일만 모으면 「지난 일」 아카이브가 된다.

원문 파싱을 켜니 미래 일정이 43건이 됐다. IR 개최일·장소, 배당 지급일,
자기주식 취득 종료일, 유상증자 납입일이 전부 원문에서만 나온다.
그래서 `--no-doc`는 개발 중 빠른 확인용이지 운영 옵션이 아니다.
CLAUDE.md에 이 사실을 적어 두었다.

## 결정 6 — 라벨 폭을 막대 배치에서 빼냈다

원래 `layout()`은 라벨 폭까지 더해 겹침을 계산했다. ppd 2.6에서 20자 제목은
약 270px, 즉 105일치 공간을 예약한다. 일정 30건짜리 프로토타입에서는 문제가
없었지만 673건이 되자 레인 하나가 수십 줄로 늘어나 화면이 무너졌다.

배치는 막대 기하로만 하고, 배치가 끝난 뒤 같은 줄에서 다음 막대까지의 간격을
보고 라벨을 그릴지 정하도록 바꿨다. 라벨이 없는 막대도 hover 툴팁과 클릭 상세,
아젠다 뷰에서 전부 읽을 수 있으므로 정보가 사라지지는 않는다.

## 결정 7 — 아젠다 뷰를 넣었다

타임라인은 「언제쯤 무엇이 몰려 있나」를 보여주지만 「내일 무슨 일이 있나」에는
약하다. 밀집 구간에서는 특히 그렇다. 날짜순 목록 뷰를 나란히 두고 A 키로
전환한다. 같은 필터·같은 데이터를 쓰므로 상태가 갈라지지 않는다.

## 결정 8 — events.json 을 화면이 fetch 하지 않는다

`fetch('events.json')` + 인라인 fallback 을 검토했다가 접었다.
`--inject`가 이미 HTML 을 갱신하고, file:// 로 열면 fetch 가 막히며,
로딩 상태·실패 처리가 새로 필요해진다. 단일 파일이라는 성질이
이 프로젝트에서는 배포 편의보다 크다. events.json 은 수집 산출물이자
다른 스크립트(`--load --brief`)의 입력으로만 쓴다.

## 결정 9 — 캘린더 알림은 확정된 내 종목에만 건다

`.ics` 내보내기에서 VALARM 은 `watch && !estimated && verified!==false`
일 때만 붙인다. 예상 일정까지 하루 전에 울리면 알림 자체를 믿지 않게 되고,
그러면 확정 일정 알림도 같이 무시된다. 예상·샘플은 제목에 표시만 남긴다.

## 남은 것

- 워치리스트 종목의 학회 참가 여부는 아직 연결되지 않았다. `static_events.json`
  의 `tickers` 는 손으로 적은 기업명이라 `watch` 플래그가 붙지 않는다.
  종목코드로 바꾸면 자동으로 이어진다.
- 2027 휴장일 표가 없어 2027년 법정기한은 전부 `estimated` 로 나간다.
  KRX 휴장일 공지가 나오면 `HOLIDAYS` 에 추가하고 `HOLIDAY_YEARS` 에 2027 을 넣는다.
- 학회 초록 공개일(엠바고 해제)은 여전히 수집원이 없다. 주가 반응의 1차 트리거라
  값어치가 큰데, 학회별 공지를 손으로 넣는 것 말고는 방법을 못 찾았다.

## 2026-08-27 (2차) — 매크로·정책 일정 추가

FOMC·옵션만기·금통위 등 미국/한국/일본 주요 일정을 넣어 달라는 요청.
이 프로젝트의 제1규칙과 정면으로 부딪히는 종류의 작업이다 — 이런 날짜는
「대충 아는」 상태로 채우기 가장 쉽고, 틀렸을 때 투자 판단에 바로 영향을 준다.

## 결정 10 — 규칙으로 계산되는 것과 공표되는 것을 갈랐다

**계산되는 것** (`expiry_events`) — 파생상품 만기는 규칙이 확정되어 있다.
한국 코스피200 선·옵은 매월 두 번째 목요일, 미국 쿼드러플 위칭은 3·6·9·12월
세 번째 금요일, 일본 메이저 SQ 는 3·6·9·12월 두 번째 금요일.

**공표되는 것** (`static_events.json`) — FOMC·금통위·BOJ 는 규칙이 없다.
각 기관이 매년 발표하는 날짜라 1차 출처에서 확인해 손으로 넣었다.

이 구분이 중요한 이유는, 계산되는 것은 코드가 매년 알아서 만들지만 공표되는
것은 사람이 매년 갱신해야 하기 때문이다. 섞어 두면 어느 쪽이 썩었는지 모른다.

## 결정 11 — 만기일은 앞으로 당기고 법정기한은 뒤로 민다

같은 「휴장일 보정」인데 방향이 반대다. 만기일이 휴장일이면 직전 거래일,
법정 제출기한이 휴일이면 익영업일. `prev_business_day` 를 새로 만들면서
`next_business_day` 와 헷갈리지 않도록 회귀 케이스를 넣었다.

실제 사례가 있다 — 2026-06-19 는 6월 세 번째 금요일이자 준틴스 휴장일이라
미국 쿼드러플 위칭이 6/18 로 당겨진다. 2027-06-18 도 준틴스 대체휴장이라
6/17 로 당겨진다. 「셋째 금요일」만 계산했으면 두 해 연속 틀렸을 것이다.
이걸 잡으려고 NYSE 휴장일 2026·2027 을 nyse.com 에서 확인해 표로 넣었다.

## 결정 12 — 검색 요약본을 1차 출처로 취급하지 않는다

BOJ 2027 일정을 처음에 웹 요약으로 받았을 때 6월 회의가 「June 8-9」로 왔다.
원문 PDF 를 열어 보니 **June 10-11** 이었다. 나머지 7개는 맞았다.
요약 모델이 FOMC 2027 의 6월(8-9일)과 섞은 것으로 보인다.

그래서 BOJ 는 boj.or.jp 의 연간 일정 PDF 두 개(2026·2027)를 직접 열어
표를 눈으로 읽고 옮겼고, FOMC 2027 은 같은 페이지를 두 번 다른 프롬프트로
읽어 두 결과가 일치하는 것을 확인했다.

## 결정 13 — 못 넣은 것을 비워 두고 문서에 남겼다

- **미국 CPI·고용지표** — BLS 가 자동 요청을 403 으로 막는다. 규칙으로도
  계산되지 않는다(매월 10~13일 근처지만 확정 규칙이 아니다). 넣지 않았다.
- **금융위원회 발표** — 연간 회의 일정을 공표하지 않는다. 정례회의가 있긴
  하지만 날짜 목록을 구할 수 없어 넣지 않았다.
- **2027 금통위** — 한국은행이 아직 공표하지 않았다(통상 전년 10월 말 발표).
- **2027 KRX 휴장일** — 아직 공표 전이라 2027년 한국 만기일·법정기한은
  전부 `estimated` 로 나간다.

「일정이 많지 않다」는 요청이었지만 채우는 것보다 비워 두는 쪽을 택한 항목들이다.
빈 자리는 CLAUDE.md 에 이유와 함께 적어 두었다.

## 결정 14 — macro 카테고리를 새로 만들었다

FOMC·금통위·BOJ 는 정책 이벤트고 옵션만기·폐장일은 시장 메커니즘이라
성격이 다르다. `market` 에 몰아넣으면 필터가 쓸모없어진다.
`macro`(매크로·정책)를 추가하고 CATS·CSS 토큰 3곳·정적 데이터를 함께 고쳤다.

CSS 토큰을 문자열 치환으로 넣다가 들여쓰기 4칸 선언이 6칸 선언의 부분문자열이라
시스템다크 블록에 중복 줄이 끼는 일이 있었다. CLAUDE.md 에 경고를 남겼다.
