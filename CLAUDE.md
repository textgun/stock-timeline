# 주식 일정 타임라인

학회·기업 IR·실적·증자·배당·공시 마감 일정을 DART에서 수집해
가로 확대축소 타임라인과 날짜순 아젠다로 보여주는 프로젝트.

```
.
├── collect_dart.py          수집기 — DART → events.json → HTML 주입
├── daily.sh                 로컬 크론용 — 수집 + 배포
├── .env                     DART_API_KEY (gitignore, .env.example 참고)
├── test_ui.py               화면 회귀 테스트 (playwright)
├── index.html               단일 파일 프론트엔드 (GitHub Pages 진입점)
├── events.json              수집 결과 (자동 생성, 커밋함)
├── watchlist.json           내 보유·관심 종목 (없으면 stock_analyzer 폴백)
├── overrides.json           손으로 고친 것 — 수집기가 절대 덮어쓰지 않는다
├── static_events.json       학회·중앙은행 정책 일정 (손으로 관리, 출처 필수)
├── data/corp_map.json       종목코드 → DART 기업코드 캐시 (gitignore)
└── .github/workflows/       평일 19시(KST) 자동 수집
```

## 배포

공개 저장소 **textgun/stock-timeline** · 화면 **https://textgun.github.io/stock-timeline/**

예전 이름은 `bio-ir-timeline` 이었다. 바이오 학회에서 시작했지만 지금은 금융지주·반도체·방산까지
26종목의 공시와 FOMC·금통위·파생 만기를 다루므로 이름을 넓혔다.
GitHub 가 옛 주소를 새 주소로 넘겨주므로 링크는 살아 있다.
`.ics` 의 UID 접미사(`@bio-ir-timeline`)는 일부러 그대로 뒀다 —
바꾸면 이미 캘린더에 넣은 일정이 갱신되지 않고 중복으로 쌓인다.

이 디렉터리는 워크스페이스 저장소의 하위 폴더지만, 배포 저장소에서는 루트다.
`git subtree split` 으로 떼어 올린다.

```bash
cd ~/.openclaw/workspace
git push --force schedule-pages "$(git subtree split --prefix=schedule)":main
```

`--force` 를 쓰는 이유 — 배포 저장소에는 수집 결과만 올라가므로 이력이 갈라질 수 있다.
`events.json` 과 `index.html` 은 둘 다 산출물이라 다음 수집이 다시 만든다.
**손으로 고친 값은 `overrides.json` 에 있으므로 강제 푸시로 날아가지 않는다.**

### 자동 수집은 로컬 크론이 한다

```
30 19 * * 1-5 /home/piction/.openclaw/workspace/schedule/daily.sh
```

`daily.sh` 가 셀프테스트 → 수집 → 급감 점검 → 커밋 → 배포까지 한 번에 한다.
로그는 `logs/collect_YYYY-MM-DD.log`.

**GitHub Actions 를 쓰지 않는 이유는 키 때문이다.** Actions 는 `.env` 를 읽을 수 없고,
공개 저장소에 `.env` 를 올리면 그게 곧 노출이다. 키를 저장소 시크릿으로 올리는 방법도
있지만, 이 워크스페이스의 다른 파이프라인(stock_analyzer)이 전부 로컬 크론으로
도는 것과 맞추고 키가 이 기계를 벗어나지 않게 했다.
`.github/workflows/collect.yml` 은 수동 실행 전용으로 남겨 두었다 —
쓰려면 `DART_API_KEY` 를 저장소 시크릿에 등록해야 한다.

## 명령

```bash
python3 collect_dart.py --selftest                      API 키 없이 규칙 점검
python3 collect_dart.py --days 180 --inject index.html  수집 + 화면 주입
python3 collect_dart.py --days 180 --no-doc             원문 파싱 생략 (빠름, 미래 일정 거의 안 나옴)
python3 collect_dart.py --load --brief 14               수집 없이 D-14 일정만 출력

python3 collect_dart.py --fix <id> start=2026-09-15 estimated=false src=https://...
python3 collect_dart.py --add start=2026-09-15 title='락업 해제' cat=cap org='에이피알'
python3 collect_dart.py --hide <id>                    화면에서만 뺀다
python3 collect_dart.py --unfix <id>                   되돌린다
python3 collect_dart.py --overrides                    걸려 있는 손질 목록
python3 collect_dart.py --load --inject index.html     손질을 화면에 반영
```

### API 키

세 곳을 순서대로 본다. 어디서 읽었는지 실행할 때 stderr 에 찍힌다.

| 순서 | 자리 | 쓰임 |
|---|---|---|
| 1 | 환경변수 `DART_API_KEY` | 일회성 실행 |
| 2 | 이 폴더의 `.env` | **평소 자리** — `.gitignore` 에 있어 커밋되지 않는다 |
| 3 | `../stock_analyzer/.env` | 같은 워크스페이스의 기존 설정 재사용 |

```bash
cp .env.example .env      # 그리고 DART_API_KEY= 뒤에 키를 붙여넣는다
```

키를 코드나 명령행에 적지 않는다. 키가 없으면 실행할 때 넣는 방법을 안내하고,
법정기한·만기일·고정 일정만 만든 뒤 계속한다 — 공시 일정만 비게 된다.

수집기의 분류 규칙이나 법정기한 계산을 고쳤으면 `--selftest`를 먼저 돌린다.
새 분류 규칙을 추가했으면 `SAMPLES`에 실제 보고서명을 한 줄 추가하고,
새 원문 파서를 추가했으면 `DOC_SAMPLES`에 실제 공시 원문 조각을 한 줄 추가한다.
정규식을 기억으로 쓰지 말고 실제 공시를 한 건 열어보고 라벨을 확인한다.

화면을 고쳤으면 `python3 test_ui.py` 를 돌리고 **종료 코드를 본다.**
출력만 훑으면 중간에 죽어도 앞부분의 「전부 통과」를 보고 넘어가게 된다.
그다음 라이트·다크 양쪽
스크린샷을 찍어 눈으로 확인한다. 테스트는 「깨지지 않았다」만 본다.
타임라인은 코드가 맞아도 화면이 읽히지 않는 종류의 UI다.

## 절대 어기지 않는 규칙

**확인하지 못한 날짜를 확정처럼 넣지 않는다.** 투자 판단에 쓰이는 화면이다.
모든 일정은 셋 중 하나다.

| 상태 | 필드 | 화면 | 캘린더 내보내기 |
|---|---|---|---|
| 확정 | `verified: true` | 실선 | 알림 붙음 (내 종목 한정) |
| 예상 | `estimated: true` | 점선 + 「예상」 | 제목에 (예상), 알림 없음 |
| 샘플 | `verified: false` | 「샘플」 | 제목에 (샘플), 알림 없음 |

두 필드는 서로 다른 것을 뜻한다. `verified` 는 **출처가 실재하는가**,
`estimated` 는 **날짜가 추정인가**. 계산으로 만든 법정기한이나 만기일,
연준이 잠정으로 공표한 FOMC 2027 은 출처가 있으므로
`verified: true` + `estimated: true` 다. 「샘플」이 아니다.
출처가 있는데 `verified: false` 로 적으면 `--selftest` 가 실패한다.
화면에서도 두 딱지는 배타적이다 — 예상이 우선한다.

날짜를 모르면 비워두거나 `estimated`로 둔다. 그럴듯한 날짜를 채우지 않는다.
수집이 실패한 항목도 조용히 빠뜨리지 않고 화면에 남긴다.

`note` 필드는 근거를 적는 자리다 — 어디서 온 날짜인지, 예상이면 무엇을 근거로 계산했는지.

휴장일 표가 없는 연도의 법정기한은 자동으로 `estimated`로 나간다.
음력 명절을 추측해서 채우느니 예상으로 표시하는 쪽을 택한 것이다.
새 연도 휴장일이 확정되면 `HOLIDAYS`와 `HOLIDAY_YEARS`에 함께 추가한다.

## 일정의 세 가지 출처

| 출처 | 무엇 | 어디서 |
|---|---|---|
| 계산 | 법정 제출기한, 파생상품 만기 | `statutory_events()`, `expiry_events()` — API 불필요 |
| 수집 | 워치리스트 종목의 공시 일정 | DART 목록 API + 공시 원문 파싱 |
| 손 | 학회, FOMC·금통위·BOJ | `static_events.json` — 1차 출처 URL 을 `src` 에 반드시 남긴다 |

**중앙은행 일정은 규칙으로 계산되지 않는다.** 각 기관이 공표하는 날짜라 손으로 넣되
반드시 1차 출처에서 확인한다. `--selftest` 가 `verified:true` 인데 `src` 가 없으면 실패시킨다.

| 기관 | 출처 | 상태 |
|---|---|---|
| FOMC | federalreserve.gov 회의 일정표 | 2026 확정 · 2027 은 연준이 잠정으로 표시 → `estimated` |
| 한국은행 금통위 | bok.or.kr 통화정책방향 결정회의 목록 | 2026 확정 · 2027 미공표 |
| 일본은행 금정위 | boj.or.jp 연간 일정 PDF | 2026·2027 확정 (매년 7월 말 다음 해분 공표) |

검색 요약본을 그대로 믿지 않는다. BOJ 2027 일정은 요약본이 6월 회의를 8-9일로 줬지만
원문 PDF 는 10-11일이었다. 날짜는 반드시 1차 문서를 눈으로 보고 옮긴다.

**미국 CPI·고용지표는 아직 없다.** BLS 가 자동 요청을 403 으로 막아 일정표를 못 가져왔다.
규칙으로도 계산되지 않으므로 추측해 넣지 않고 비워 두었다.
`금융위원회`는 연간 회의 일정을 공표하지 않아 마찬가지로 넣지 않았다.

## 값이 애매할 때 — 손으로 고친다

자동 수집이 못 맞히는 것이 늘 남는다. 원문 서식이 바뀌어 날짜를 놓치거나,
휴장일 표가 없어 예상으로 나가거나, 아예 수집원이 없는 일정(미국 CPI 등)이 그렇다.
`overrides.json` 이 그것을 받는 자리다.

```json
{
 "fix":  { "exp-kr-2027-03": { "start": "2027-03-11", "estimated": false, "src": "..." } },
 "hide": ["d20260101000001"],
 "add":  [{ "start": "2026-09-15", "title": "락업 해제", "cat": "cap", "org": "에이피알" }]
}
```

**수집기는 이 파일을 절대 쓰지 않는다.** 읽기만 한다. CLI 로 고치거나 직접 편집한다.
id 는 화면에서 일정을 클릭하면 상세 아래에 나오고, 「고치기 명령 복사」를 누르면
현재 값이 채워진 명령이 그대로 복사된다.

**되돌릴 수 있어야 한다.** 덮어쓰기 전 값을 이벤트의 `_orig` 에 넣어 둔다.
`overrides.json` 에서 항목을 지우고 `--load` 만 하면 다시 수집하지 않아도 원래 값으로 돌아간다.
`--selftest` 에 왕복 회귀 테스트가 있다.

**숨김은 삭제가 아니다.** `hidden: true` 를 달 뿐 데이터는 `events.json` 에 남는다.
화면과 캘린더 내보내기에서만 빠진다. 되살리려면 `--unfix` 하나면 된다.

**`add` 가 직접 추가 일정의 유일한 출처다.** `events.json` 은 누적되므로,
손질을 얹기 전에 기존 `manual` 항목을 먼저 걷어낸다. 그래야 `add` 에서 지운 것이
화면에서도 사라진다. 이것도 회귀 테스트가 있다.

손댄 일정은 화면에 「수정」·「직접」 딱지가 붙고, 상세에 무엇이 어떻게 바뀌었는지
원래 값과 함께 나온다. 사람이 고친 값을 조용히 섞어 두지 않는 것이 요점이다.

## 만기일 계산

| 시장 | 규칙 | 휴장일 보정 |
|---|---|---|
| 한국 코스피200 선·옵 | 매월 두 번째 목요일 (3·6·9·12월은 네 마녀의 날) | 직전 거래일 |
| 미국 쿼드러플 위칭 | 3·6·9·12월 세 번째 금요일 | 직전 거래일 |
| 일본 메이저 SQ | 3·6·9·12월 두 번째 금요일 | 휴장일 표 없음 → `estimated` |

만기일은 **휴장일과 겹치면 앞으로 당겨진다.** 법정기한이 뒤로 밀리는 것과 반대 방향이라
`prev_business_day` 와 `next_business_day` 를 섞어 쓰지 않도록 주의한다.
2026-06-19(6월 셋째 금요일)가 준틴스 휴장이라 미국 만기가 6/18 로 당겨지는 것이 실제 사례이고,
`--selftest` 에 회귀 케이스로 들어 있다.

NYSE 휴장일은 2026·2027 을 nyse.com 에서 확인해 넣었다. KRX 2027 휴장일은 아직 공표 전이라
2027년 한국 만기일과 법정기한은 전부 `estimated` 로 나간다.

## 알아둘 것

**공시의 `rcept_dt`는 접수일이지 일정일이 아니다.** 접수일만 모으면 미래 일정이
거의 안 나온다 — 실제로 `--no-doc`로 180일을 훑으면 600건 넘는 공시 중 미래
일정은 10여 건뿐이다. 미래 일정의 사실상 유일한 출처는 **공시 원문 파싱**이다.

**원문에서 뽑는 날짜와 그 라벨.** 전부 실제 공시로 확인한 것이다.

| 공시 | 라벨 | 얻는 것 |
|---|---|---|
| 기업설명회(IR)개최 | `1. 일시` / `시작시간 종료시간` | IR 개최일 + 장소 |
| 결산실적공시예고 | `3. 결산실적 공시예정일` | 실적 발표 예정일 |
| 현금ㆍ현물배당결정 | `6. 배당기준일`, `7. 배당금지급 예정일자` | 기준일 → 지급일 |
| 주주총회소집공고 | `1. 일시:` | 주총일 + 장소 |
| 자기주식취득결정 | `3. 취득예상기간 시작일/종료일` | 취득 구간 |
| 유상증자·증권신고서 | `청약기일`, `9. 납입일`, `12. 신주의 상장 예정일` | 청약 → 납입 → 상장 |

**산문으로 된 공시는 파싱하지 않는다.** 증권신고서는 서식이 아니라 투자설명서라
「납입일」이 문장 한복판에 나온다. 라벨 뒤 첫 날짜를 집으면 3년 전 연혁 표의 날짜를
가져온다 — 실제로 SK하이닉스 건에서 2023-02-01 이 확정 일정으로 들어갔다.
`RULES` 에서 kind 를 `None` 으로 두어 접수일 이벤트로만 남긴다.
그래서 유상증자 **청약일은 지금 비어 있다.** 주요사항보고서 서식에는 없는 값이다.

**뽑은 날짜가 접수일에서 너무 멀면 버린다.** `sane()` 이 접수일 기준 -540일 ~ +1100일
밖의 값을 걸러내고 stderr 에 남긴다. 원문 파싱이 어긋나는 방식이 대개 「엉뚱한 표의
날짜를 집는 것」이라 이 창 하나로 대부분 걸린다. 2026-03 공시가 알리는
2025-12-31 배당 기준일 같은 정상적인 과거 날짜는 통과한다.

**정정·철회 공시는 값이 전부 `-`다.** 라벨 뒤에서 무작정 첫 날짜를 집으면
옆 필드(이사회결의일 등)를 잘못 가져온다. `date_after`가 다음 번호 항목에서
문자열을 자르는 이유다. 이 동작은 `DOC_SAMPLES`에 회귀 테스트가 있다.

**일정이 아닌 공시가 훨씬 많다.** 임원 지분 신고 하나가 한 종목에서 176건씩
나온다. `NOISE`에서 먼저 걷어낸 뒤 `RULES`를 태운다. 채무증권 증권신고서도
주식 일정이 아니라 제외 대상이다.

**같은 일정이 여러 번 공시된다.** `[기재정정]`, 지주회사의 `(자회사의 주요경영사항)`.
`dedupe`가 (기관, 시작, 종료, 제목)이 같으면 하나로 접고 몇 건을 접었는지 `note`에 남긴다.

**원문 파싱은 서식이 바뀌면 깨진다.** 실패해도 죽지 않고 stderr에 로그만 남긴다.
정기적으로 로그의 `원문 실패` 줄을 확인해 정규식을 손보는 것을 전제한 설계다.

## 아키텍처 원칙

데이터는 `EVENTS` 배열 하나로 흐른다. 수집기가 만들고 페이지가 읽는다.
그 사이에 DB도 서버도 없다. GitHub Pages + Actions로 월 0원 운영이 전제다.
이 단순함을 깨는 변경은 하지 않는다.

**연쇄 일정도 배열을 평평하게 유지한다.** 청약→납입→상장처럼 한 사건이 여러
날짜를 가질 때 중첩 필드를 쓰지 않는다. 각 단계가 독립 이벤트가 되고
`chain`(묶음 id)과 `chainStep`(단계 이름)으로 서로를 가리킨다.
덕분에 필터·정렬·D-day 계산이 단계를 몰라도 된다.

**라벨 폭을 배치에 넣되 줄 수에 상한을 둔다.** 상한 없이 넣으면 일정이 많아질수록
레인이 수십 줄로 늘어나고, 아예 빼면 세로 공간이 남는데도 라벨이 하나도 안 붙는다.
`assign()`은 라벨까지 들어갈 자리를 `MAXROWS`(8줄) 안에서 먼저 찾고,
없으면 라벨을 포기하고 막대만 놓는다.

**한글 글자 폭을 영문 기준으로 잡지 않는다.** 11.5px 폰트에서 한글 한 글자는
거의 11.5px 를 차지한다. 6.8px 로 뭉뚱그렸다가 한글 제목 라벨이 서로 겹쳤다.
`textW()` 가 한글·한자와 영문·숫자를 나눠 센다.

**축의 왼쪽 끝은 오늘이다.** `setRange()` 가 `state.hidePast` 에 따라 축 범위를 잡는다.
기본값(지난 일정 숨김)에서 `RANGE.s = today` 이므로 `x(today) = 0`, 오늘이 왼쪽 끝에
붙고 화면 폭 전부가 앞으로의 일정에 쓰인다. 지난 일정을 켜면 그만큼 왼쪽으로 늘어난다.

`setRange()` 는 **`hidePast` 에만 반응한다.** 검색이나 카테고리 필터에도 다시 계산하면
글자를 칠 때마다 축이 늘었다 줄었다 하며 화면이 발밑에서 움직인다.

축 왼쪽 끝에서 잘린 첫 달의 라벨은 0 으로 당겨 붙이되, 다음 달 눈금이 34px 안쪽이면
아예 뺀다. 안 그러면 「8」과 「9」가 겹쳐 붙는다.

아젠다에는 「오늘」 기준선이 있다. **아직 시작하지 않은 첫 일정** 앞에 온다 —
`isPast` 로 잡으면 진행 중인 자기주식 취득 구간(시작일이 과거)에 기준선이 박힌다.

아젠다는 페이지가 아니라 **자기 안에서 스크롤한다**. 페이지째 내리면
툴바가 화면 밖으로 나가 필터를 못 만진다.

**캘린더 내보내기는 지난 일정을 뺀다.** 화면 필터를 따르되 이미 끝난 것은
제외한다. 지난 일정 133건이 캘린더에 들어가면 지우기가 번거롭다.

**밀집 구간은 묶는다.** 그래도 한 레인이 `MAXROWS`(8줄)를 넘으면 겹칠 수밖에
없는 막대들을 개수 마커 하나로 합친다. 확대하면 저절로 풀린다. 180일에 점
이벤트 166개는 어떤 배치로도 각 줄에 못 놓는다 — 줄여서 보여주는 게 아니라
지금 확대 수준에서 표현할 수 없다는 사실을 그대로 표시하는 것이다.
`test_ui.py`가 모든 확대 수준에서 마커 개수 총합이 실제 일정 수와 같은지 검사한다.

카테고리를 추가할 때는 세 군데를 같이 고친다 — HTML `CATS`, CSS `--c-*` 토큰
(라이트·시스템다크·명시다크 **세 곳 모두**), 수집기 `RULES`.
CSS 토큰을 문자열 치환으로 넣지 말 것. 들여쓰기가 다른 같은 선언이 서로의 부분문자열이라
시스템다크 블록 안에 중복 줄이 끼어든다. 실제로 한 번 겪었다.
